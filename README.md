# RMC

This repository previously contained an options trade-idea alert system for
small accounts. That project has been scrapped and its code removed; the
full history is still available in git log if any of it is needed again.
